# W7-T05 independent review — 2026-09-13

Reviewer: Codex (GPT-5), OpenAI Codex
Reviewed commit: `9bc3f9ec110c141b1a92ef30c9a55948bf77fa79`
Initial verdict: **FAIL**

Initial counts:

- BLOCKING: 1
- P1: 1
- P2: 1
- ACCEPTED-RISK: 3

W7-T15 remains blocked until an independent rerun confirms that no BLOCKING finding remains.

## HAA-REV-001 — retired authority key can mint fresh detached grants

Original severity: `BLOCKING`

Disposition: **REMEDIATED — AWAITING INDEPENDENT RERUN**

Remediation:

- live `ExecutionGrant` verification now requires the referenced authority key to be `ACTIVE`;
- RETIRED keys remain available for historical verification, but cannot authorize live detached execution;
- verifier now checks timestamp ordering, future issuance, key-created-at boundary and maximum execution-grant TTL;
- regression coverage reproduces a grant minted after retirement with the retained private key and requires `AUTHORITY_KEY_NOT_ACTIVE`.

Primary files:

- `packages/sdk-ts/src/index.ts`
- `tests/sdk-grant-verification.test.ts`
- `packages/sdk-ts/README.md`

Security rule:

```text
Historical verification may retain RETIRED public keys.
Live ExecutionGrant authority requires ACTIVE key status.
```

## HAA-REV-002 — authenticator revocation did not invalidate unconsumed approval

Original severity: `P1`

Disposition: **REMEDIATED — AWAITING INDEPENDENT RERUN**

Remediation:

- before issuing/consuming a new grant, `authorizeAndConsume` resolves the approval receipt's `authenticatorId`;
- the authenticator must still be `ACTIVE`;
- if it is missing/revoked, HAA materializes `APPROVED -> REVOKED`, writes a `REVOKED` audit event with `reason=AUTHENTICATOR_REVOKED`, and denies execution;
- the receipt is retained as historical evidence that approval was once valid;
- an already CONSUMED request/grant preserves existing idempotency semantics.

Primary files:

- `apps/haa-server/src/application.ts`
- `tests/service.test.ts`

## HAA-REV-003 — signed but undisplayed ActionProfile fields

Original severity: `P2`

Disposition: **REMEDIATED — AWAITING INDEPENDENT RERUN**

Remediation:

- built-in profiles enforce explicit field allowlists;
- unknown payload/precondition fields fail closed;
- `demo.action.v1` displays the accepted `version` precondition as `EXPECTED VERSION`;
- `git.merge.v1` rejects unexpected precondition fields;
- ActionSpec envelope fields are checked at the core profile boundary as well as at HTTP validation.

Primary files:

- `packages/core/src/actions.ts`
- `tests/core.test.ts`

## HAA-REV-004 — uncheckpointed audit tail / admin audit boundary

Original severity: `ACCEPTED-RISK`

Disposition: **ACCEPTED-RISK — UNCHANGED**

Reason:

The limitation is explicitly documented: signed checkpoints only anchor history up to the checkpoint, and `admin_audit_events` is a separate operational stream. Production guidance requires external retention of signed checkpoints when stronger DBA resistance is needed.

Future hardening may independently anchor administrative audit if required.

## HAA-REV-005 — trusted provisioning / lack of platform attestation

Original severity: `ACCEPTED-RISK`

Disposition: **ACCEPTED-RISK — UNCHANGED**

Reason:

Human/client provisioning remains an explicit administrative trust boundary and HAA does not claim Secure Enclave platform attestation. This is intentionally scoped in `docs/SECURITY.md` and `docs/AUTHENTICATOR-ASSURANCE.md`.

## HAA-REV-006 — edge profile relies on external ingress; dev bootstrap exposure

Original severity: `ACCEPTED-RISK`

Disposition: **PARTIALLY REMEDIATED / RESIDUAL ACCEPTED-RISK**

Remediation:

- `HAA_DEV_BOOTSTRAP=1` is now rejected when `HAA_NETWORK_PROFILE=edge`;
- an operator must additionally set `HAA_ALLOW_UNSAFE_DEV_BOOTSTRAP_EDGE=1` to deliberately bypass that guard;
- tests cover the fail-closed default.

Residual accepted boundary:

- edge mode still relies on an operator-controlled ingress/service mesh for TLS, rate limiting, ACLs and secret isolation, as documented.

Primary files:

- `apps/haa-server/src/network.ts`
- `apps/haa-server/src/server.ts`
- `tests/network-profile.test.ts`

## Required independent rerun

The independent reviewer should review the remediation delta from:

```text
base: 9bc3f9ec110c141b1a92ef30c9a55948bf77fa79
head: current main
```

At minimum it must attempt to reproduce:

1. post-retirement detached grant forgery;
2. execution after authenticator revocation but before consume;
3. hidden/unknown ActionProfile fields;
4. edge-mode development bootstrap without the explicit unsafe override.

Closure condition:

```text
BLOCKING = 0
W7-T05 = PASS or PASS_WITH_FOLLOWUPS
```

Only after that result may W7-T15 be considered for `DONE`.
