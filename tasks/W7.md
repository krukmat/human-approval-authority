# W7 — Packaging & Optional Adapters

W7 starts only after the real W4 agent scenario; hardware completion is optional unless W4-T06 selected that path.

- **W7-T01 — Protocol/version compatibility freeze.** Publish golden protocol fixtures and fail-closed version negotiation rules.
- **W7-T02 — Docker/self-host package.** Minimal server image, externalized authority key/secret material, persistent SQLite volume, no debug defaults.
- **W7-T03 — Publishable TypeScript SDK.** Stable typed public surface and compiling examples; do not publish externally without explicit approval.
- **W7-T04 — Optional WebAuthn adapter spike.** Must map through EvidenceVerifier without changing core; do not describe WebAuthn authentication as a legal signature.
- **W7-T05 — Final cross-model security/architecture review.** Trace every invariant to code/tests, list residual limitations and produce release blockers only; no scope expansion.
