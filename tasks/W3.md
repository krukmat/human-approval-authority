# W3 — Apple Authenticator

## Goal

Prove the universal HAA protocol with a real device-bound Mac authenticator before any hardware investment.

## Tasks

### W3-T01 — macOS approver app scaffold — DONE
Dependency: W2-T05.

### W3-T02 — Secure Enclave authenticator enrollment — DONE_UNVALIDATED
Dependencies: W3-T01, W2-T04.
Acceptance: P-256 private key remains device-bound; public key registers with HAA; biometric-set changes require re-enrollment as designed.

### W3-T03 — Signed package verification + trusted renderer — DONE_UNVALIDATED
Dependencies: W3-T01, W1-T02, W1-T04.
Acceptance: invalid/expired HAA package fails closed; renderer uses only signed structured claims; unsupported profile is rejected.

### W3-T04 — Touch ID-gated approval signature — DONE_UNVALIDATED
Dependencies: W3-T02, W3-T03.
Acceptance: approve signs only after explicit user action and Touch ID; reject/timeout create no evidence.

### W3-T05 — Apple evidence verifier adapter — DONE
Dependencies: W2-T06, W3-T04.
Acceptance: core contracts unchanged; wrong key/revoked authenticator rejected; VerifiedEvidence stays platform-neutral.

### W3-T06 — Apple end-to-end gate — BLOCKED
Dependencies: W3-T05, W2-T09.
Run on the target Mac using `docs/MACOS-RUNBOOK.md`.
Acceptance: real Touch ID reaches APPROVED; post-display action mutation fails; receipt/grant lifecycle remains separated; audit identifies the Mac authenticator.
