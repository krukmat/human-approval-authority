# W4 — Agent Integration & Value Gate

## Goal

Demonstrate that a real agent workflow is blocked until human approval and that HAA authorizes only the exact action.

### W4-T01 — TypeScript requester SDK — DONE
Dependency: W2-T09.

### W4-T02 — CLI approval requester — DONE
Dependency: W4-T01.

### W4-T03 — MCP adapter — DONE_UNVALIDATED
Dependency: W4-T01.
Acceptance: tools map to typed ActionSpec profiles; MCP cannot self-approve; status supports asynchronous human workflow; no generic execute tool.

### W4-T04 — Reference executor + precondition enforcement — DONE
Dependencies: W2-T07, W4-T01.
Acceptance: receipt alone is insufficient; stale precondition fails; execution IDs are idempotent; no arbitrary shell execution.

### W4-T05 — Real agent approval scenario — BLOCKED
Dependencies: W3-T06, W4-T02, W4-T03, W4-T04.
Acceptance: agent cannot progress before approval; human sees deterministic summary; changed action fails; successful action consumes approval once.

### W4-T06 — Value-gate decision record — BLOCKED
Dependency: W4-T05.
Hardware may start only when the real workflow establishes value for at least one of: host separation, trusted dedicated display, portability/vendor neutrality or a physical human gate.
