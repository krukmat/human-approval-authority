# W0-W2 — Foundation, Core and Service

These waves are complete in the validated workspace and represented by executable tests.

## W0 — Foundation & Contracts

- W0-T01 monorepo/package-boundary scaffold.
- W0-T02 versioned domain/protocol schema freeze.
- W0-T03 test/CI harness.
- W0-T04 threat-case catalogue.

## W1 — Universal Core

- W1-T01 deterministic canonicalization and separate action/intent digests.
- W1-T02 versioned ActionProfile registry and deterministic display/preconditions.
- W1-T03 approval lifecycle state machine.
- W1-T04 fresh-nonce signed ApprovalChallengePackage.
- W1-T05 authenticator registry and technology-neutral EvidenceVerifier port.
- W1-T06 ApprovalReceipt vs short-lived ExecutionGrant separation.
- W1-T07 security integration tests.

## W2 — HAA Service & Enforcement

- W2-T01 SQLite transactional adapter.
- W2-T02 authenticated requester/client identity.
- W2-T03 approval request API.
- W2-T04 authenticator enrollment/revocation API.
- W2-T05 challenge issuance.
- W2-T06 evidence verification → APPROVED + receipt.
- W2-T07 atomic authorizeAndConsume with idempotent execution IDs.
- W2-T08 append-only audit flow.
- W2-T09 end-to-end security gate.

Do not reopen stable contracts during later implementation unless a security invariant cannot be satisfied; record such a change as an explicit architecture decision first.
